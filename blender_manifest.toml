schema_version = "1.0.0"

id = "autoremesher_bridge"
version = "0.1.0"
name = "AutoRemesher Bridge"
tagline = "Run AutoRemesher on the active mesh"
maintainer = "Adriflex"
type = "add-on"

blender_version_min = "4.2.0"
license = ["SPDX:GPL-3.0-or-later"]
